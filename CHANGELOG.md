# Changelog

本项目记录面向使用者的变更。未发布计划见 [Roadmap](ROADMAP.md)。

## 未发布 — 2026-09-10

### 账号与持久化

- 新增可选用户名账号、HttpOnly 会话和 SQLite 游戏库快照；登录后自动合并恢复 ROM、游戏内存档与即时存档，并继续保留 IndexedDB 离线使用。
- 游戏导入、删除、元数据与存档写入会自动触发延迟同步，切到后台或网络恢复时立即补传；已登录的其他浏览器会定时及在返回前台时拉取新版本，无需手动点击同步。账号库按游戏分片传输，可同步数百个游戏而不受单份手工备份的 16 个游戏限制；退出账号不删除浏览器本地游戏。
- 新增同源请求校验、scrypt 密码哈希、登录限流、72 MiB 快照限制及账户 API 集成测试。
- 新增同源生产服务 `pnpm start`；纯静态部署仍只支持本地持久化。
- 生产服务支持通过 `TLS_CERT_PATH` 与 `TLS_KEY_PATH` 直接启用 HTTPS，并可配置 `HOST` 监听地址。

### 平台支持

- 新增 Game Boy 与 Game Boy Color ROM 运行，支持 `.gb`、`.gbc` 及混合 ZIP 导入。
- 新增 FC / NES 与 SFC / SNES 单人运行，支持 `.nes`、`.sfc`、`.smc` 及混合 ZIP 导入；核心资源固定在本地，无运行时 CDN 请求。
- SFC / SNES 导入会从有效 LoROM、HiROM、ExHiROM 及带复制头的 ROM 中读取内部游戏名；已有数字文件名标题会自动修复，同时保留用户自定义标题。
- 游戏库新增五个平台的标签与筛选；播放器按平台注册表使用原生画面比例。
- GB / GBC 隐藏无效的 L / R 触屏肩键；存档、截图、倍速、倒带及键盘 / 手柄输入沿用现有 mGBA 流程。
- SFC 增加 X / Y / L / R 键盘、手柄和四键菱形触屏输入；FC 按平台隐藏无效按键。双人输入尚未实现。
- 平台标识、文件扩展名、原生分辨率、输入差异和核心标识集中管理；不同核心的即时存档保持隔离。

### FC / SFC 核心与验证

- 固定 EmulatorJS 4.2.3、FCEUmm 4.2.3 与 Snes9x 4.2.3 的非线程 WebAssembly 资源，并记录来源、许可证和 SHA-256。
- 增加 iNES 文件头与 SFC 镜像大小校验，以及运行时生成的原创最小 NES / SNES 测试 ROM。
- 浏览器回归覆盖真实核心启动、快速存档与缩略图、平台切换、额外按键、刷新持久化和移动端溢出。
- 修复单页应用切换核心时的监听器与 WASM 生命周期释放，以及可选倒带入口缺失导致的应用卸载。

### GB / GBC 验证与兼容

- 旧版 GBA 游戏库记录会按文件名补全平台信息，无需重新导入。
- GBA 保留原有 ROM SHA-256 标识；GB / GBC 使用平台域内容标识，使相同字节按不同平台导入时不会共享游戏或存档。
- 新增 GBA / GB / GBC 尺寸校验、混合 ZIP 单元测试，以及原创最小 GB / GBC ROM 的真实 mGBA 与响应式 UI 回归。
- 自动化 GB / GBC ROM 使用 MBC1 与 8 KiB 电池 RAM；其他 mapper、RTC、实体设备和完整商业 ROM 兼容性仍待分别验证。

### 图形兼容模式

- WebGL 2 不可用时自动切换到 Canvas 2D 软件渲染，保留现有 mGBA 核心、音频、输入、即时存档、截图与电池存档流程；WebGL 2 可用时仍使用原有加速路径。
- 环境检查区分 WebGL 2、浏览器软件 WebGL、仅 WebGL 1、Canvas 2D 降级及完全无图形上下文，并把可降级环境显示为可启动的性能警告。
- 新增真实核心回归：强制禁用 WebGL 后，原创 GBA / GB / GBC ROM 均产生非空画面并保持可玩帧率，切换平台、销毁 worker 及在同一画布重建核心通过。

### v1.3 可安装与离线应用壳

- 新增 Web App Manifest 与版本化 Service Worker，首次联网后缓存应用壳、字体和全部模拟核心资源；断网时保留 COOP / COEP 响应头并回退到已缓存页面。
- 显示在线、离线、本地缓存和可用更新状态；更新需用户确认，运行中的游戏会先暂停后再切换页面。
- 新增 `pnpm verify:core` 校验随仓库分发的 mGBA、EmulatorJS 前端与 FC / SFC 核心资源 SHA-256，并加入 CI。

### 游戏库备份与恢复

- 新增侧栏「备份与恢复」：选择游戏导出版本化 ZIP，默认包含元数据与存档，可选择包含 ROM；文件带大小与 SHA-256 校验。
- 导入前只读预览，显示缺少的 ROM、游戏和槽位冲突、即时存档核心来源。默认保留本地，支持逐项选择备份，并在内存中匹配缺失 ROM。
- 一次数据库事务恢复选中项目；失败整体回滚，预览过期时要求重新预览。恢复成功后关闭旧运行实例，避免旧进度再次覆盖。
- 显示站点估计用量与游戏关联存档数量，整理使用已有删除确认；配额不可用时仍可使用备份。
- 新建即时存档记录稳定核心标识，旧存档来源未知并默认跳过恢复；未升级内核、数据库版本或单游戏存档格式。
- 只恢复电池进度时保留旧自动档，并持久化跳过旧自动续玩的标记，防止刷新后旧即时档覆盖刚恢复的进度。

v1.1 的输入配置与可靠性实现；跨浏览器、真机性能和辅助技术验收仍有未完成项，见 [阶段记录](docs/roadmaps/v1.1.md)。

### 输入与操作

- 新增按设备保存的手柄按钮 / 轴方向映射、摇杆死区、取消录入、清除单项和恢复默认。标准手柄保留原默认布局，非标准设备从空映射开始配置。
- 新增标准 / 紧凑触屏布局、按键大小与不透明度设置；刷新后保留，旧配置自动补齐默认值。
- 多指触控与键盘 / 手柄共同持有同一按键时正确保留输入；断连、失焦、弹窗、暂停、触控取消和横竖屏切换释放相应按键。
- 游戏快捷键仅在画面聚焦时生效；改进启动、退出和对话框焦点恢复，保留正常界面键盘导航，映射录入可通过 Esc 取消。

### 启动与进度保护

- 启动和电池存档导入等待当前游戏真正完成首帧，保留超时、加载取消及资源释放处理，避免过早读取尚未初始化的进度。
- 电池存档从核心原生即时快照读取当前 SRAM，避免游戏立即暂停后导出尚未写回虚拟文件系统的旧内容；校验快照边界和解压长度。
- 2026-09-11：适配层在短暂停止原生 CPU 时存取即时档、截图、捕获 SRAM 和重置；嵌套捕获不会提前恢复运行，原已暂停的游戏保持暂停。导入电池存档重启前先暂停 CPU，再销毁旧音频资源；未更换随仓库分发的核心。
- 已定位 MEMFS 同步的主线程代理互锁：主线程等待 CPU 暂停，而 CPU 等待被主线程阻塞的 Promise 微任务。调整 JavaScript 的文件同步与代理完成入口，使纯内存同步立即完成，保留异步文件系统的 Promise 路径；新增同步边界回归。`mgba.wasm` 仍为未修改的 2.5.1。
- 增加运行前提与存储异常的回归覆盖。存储估算失败不应阻止读取已有游戏库。
- 未升级随仓库分发的 mGBA 核心，未改变对外 `.sav` / 即时存档格式；原生电池快照提取只处理核心生成的数据。

### 验证

- 新增 `test:keyboard`、`test:gamepad`、`test:touch` 和 `test:startup` 浏览器套件，以及输入配置、来源管理和电池快照单元测试。
- 兼容性记录区分 Windows Edge 自动化、模拟 Gamepad API、合成指针和模拟视口。实体手柄、Firefox、Safari、Android / iOS、低性能设备、音频试听与屏幕阅读器检查保持待验证。

## 1.0.0 — 2026-09-09

首次公开开源版本。

### 游戏与界面

- 基于 mGBA WebAssembly 的 GBA 游戏运行、声音、暂停、重启和全屏。
- 中文深色界面、原创掌机插画、响应式游戏库与移动触屏手柄。
- `.gba` / `.zip` 导入、子目录解压、内容去重、收藏、搜索、排序及游玩记录。
- 1× / 2× / 4× 运行速度、倒带、像素 / 平滑 / CRT 画面与截图。
- 可重新映射的键盘输入及标准手柄支持。

### 进度管理

- 五个手动即时存档位与一个自动位，包含画面预览。
- 即时存档和 `.sav` 电池存档导入 / 导出，IndexedDB 本地持久化。
- 游戏切换、后台暂停及存档导入后的进度保护。
- 修复多次载入游戏时旧音频回调访问已释放内存的问题。

### 示例与开发

- 原创、可复现生成的 Star Orbit GBA 试玩，含移动、信标收集、音效和 SRAM 存档。
- 存储和 ZIP 单元测试，以及真实内核、完整界面和存档流程的浏览器回归。
- 首页面向开发者的真实截图、安装部署说明、贡献指南与第三方许可声明。

### 当前限制

需要 HTTPS / localhost 和 COOP / COEP 响应头。暂不提供公开在线试玩、PWA 离线安装、联机、作弊码或云同步；不包含商业游戏 ROM。
